ServerEvents.recipes(e => {
    let sets = [
        ""
    ].forEach((set) => {
        let armorTypes = [
            "helmet",
            "chestplate",
            "leggings",
            "boots"
        ].forEach((piece) => {
            
        })
    })
})